# Not needed for local dev — vite.config.ts already proxies /api to the local
# server. Set this only for a production build, to the deployed server's URL.
# VITE_API_URL=https://your-server.onrender.com
